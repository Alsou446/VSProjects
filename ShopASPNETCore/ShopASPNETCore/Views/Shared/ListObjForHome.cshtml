@model ShopASPNETCore.Data.Models.TasksList

<label class="list-group-item rounded-3 py-3" for="listGroupCheckableRadios1">
    <h3 color="black">@Html.RouteLink( @Model.Name, "curTasksList", new { tskListName = @Model.ListName})</h3>
    <a asp-controller="TasksList" asp-action="DelList" class="btn btn-primary w-20" asp-route-id="@Model.Id" style="position:absolute; right:3%; top:25%">Удалить</a>
 </label>
